export default function Footer() {
  return (
    <footer className="border-t border-line mt-16">
      <div className="container-page py-8 text-sm text-ink/40 flex items-center justify-between font-mono">
        <span>VendEx — stall directory</span>
        <span>open daily, no membership required</span>
      </div>
    </footer>
  );
}
